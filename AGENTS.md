# AGENTS.md

## 适用范围

- 本文件适用于 `Packages/Mochi` 整个 Git 仓库。
- Mochi 是独立可复用的**纯 C# 核心**框架；
- UPM 包名为 `com.mochi.core`，程序集名为 `Mochi`。

## 目录职责

- `Base/`：观察值、单例等基础抽象。
- `DataTable/`：通用数据表及其注册、查询逻辑。
- `EventSystem/`：类型安全的事件定义、注册和发布。
- `Logging/`：日志级别、日志条目、记录器与输出接口。
- `StateMachine/`：普通及层级状态机。
- `Util/`：不属于具体模块的通用数据结构和值对象。
- `Mochi.asmdef`：程序集边界；`package.json`：UPM 包身份、版本与最低 Unity 版本。

## 架构边界

- 不引用任何游戏引擎
- 保持各模块低耦合。公共抽象优先放在拥有该概念的模块中，不要为单一调用方扩大核心 API。

## 实现约定

- 使用仓库现有 C# 风格：4 空格缩进、Allman 大括号、类型和公开成员用 PascalCase、局部变量和参数用 camelCase。
- 命名空间沿用按照模块划分。
- 新增行为时优先保持 API 小而明确；空值、重复注册、非法状态转换和释放后的调用要有可预测结果。

## Unity 与包文件

- 新增、移动或删除 Unity 可见文件/目录时，同步处理对应 `.meta` 文件；不要随意重建已有 GUID。
- `package.json` 必须保持合法 JSON。只有发布语义发生变化时才更新版本，并同步核对下游包的 `com.mochi.core` 依赖版本。
- 修改 `Mochi.asmdef` 时保持程序集名称兼容，并确认没有意外增加引擎或第三方程序集依赖。

## 测试与验证

- 修复缺陷或改变行为时，应先添加能复现问题的最小自动化测试；若仓库尚无合适测试程序集，可在 `Tests/` 下建立独立测试程序集并引用 `Mochi`。
- 涉及包清单、程序集定义、序列化或 Unity 消费端时，还要让 Unity 完成导入/编译；涉及公共 API 时，至少确认`Mochi`系列包的编译没有新增错误。

## 版本控制

- 本目录是独立 Git 仓库；
- 只提交本任务相关文件，保留用户已有改动；提交前检查版本控制状态。
- 不提交 Unity/IDE 生成物。提交说明沿用简洁的 Conventional Commits 风格，例如 `fix: ...`、`feat: ...`、`refactor: ...`、`test: ...`、`docs: ...`、`chore: ...`。